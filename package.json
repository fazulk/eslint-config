{
  "name": "@fazulk/eslint-config-monorepo",
  "version": "0.35.5",
  "private": true,
  "packageManager": "pnpm@7.1.0",
  "author": "Jeff Fasulkey (https://github.com/fazulk/)",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "test": "pnpm -r run test",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm publish --filter '@fazulk/eslint-config-vue2' '@fazulk/eslint-config-svelte' --access public"
  },
  "devDependencies": {
    "@antfu/eslint-config": "*",
    "bumpp": "^8.2.1",
    "eslint": "^8.33.0",
    "eslint-plugin-antfu": "workspace:*",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.4"
  }
}
